# Public assets

- Brand/logo (prints top-right): `public/logo.png` (also supports `.jpg`/`.jpeg`)
- Event logo (prints top-left): `public/event_logo.jpeg` (also supports `.png`/`.jpg`/`.webp`)
- The PDF renderer converts both to pure black/white automatically.
- Replace these files with your real logos (keep the same filenames).
