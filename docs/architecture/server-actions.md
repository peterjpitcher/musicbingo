---
generated: true
last_updated: 2026-05-11T00:00:00Z
source: session-setup
project: music-bingo
---

# Server Actions

> Auto-generated by session-setup. Manual edits will be overwritten on next refresh.

## Status

This project does **not use Next.js server actions** (`'use server'` directive). All mutations are performed through [[routes#API Routes|API route handlers]].

The data access layer is implemented as plain async functions in `lib/` that are called by the API routes:

### Session Repository (`lib/live/sessionRepo.ts`)

| Function | Tables | Purpose |
|----------|--------|---------|
| `listSessions` | [[data-model#live_sessions\|live_sessions]] | List all sessions ordered by `updated_at` |
| `getSession` | [[data-model#live_sessions\|live_sessions]] | Get single session by ID |
| `upsertSession` | [[data-model#live_sessions\|live_sessions]] | Create or update session (with brand_id) |
| `deleteSession` | [[data-model#live_sessions\|live_sessions]] | Delete session by ID |
| `getRuntimeState` | [[data-model#live_sessions\|live_sessions]] | Get runtime state from session row |
| `upsertRuntimeState` | [[data-model#live_sessions\|live_sessions]] | Update runtime state on session row |

### Brand Repository (`lib/brands/brandRepo.ts`)

| Function | Tables | Purpose |
|----------|--------|---------|
| `listBrands` | [[data-model#brands\|brands]] | List all brands ordered by name |
| `getBrand` | [[data-model#brands\|brands]] | Get brand by ID |
| `getDefaultBrand` | [[data-model#brands\|brands]] | Get brand where `is_default = true` |
| `getEffectiveBrandConfig` | [[data-model#brands\|brands]] | Get brand config with fallback to default |
| `createBrand` | [[data-model#brands\|brands]] | Create brand (manages `is_default` uniqueness) |
| `updateBrand` | [[data-model#brands\|brands]] | Update brand (manages `is_default` uniqueness) |
| `deleteBrand` | [[data-model#brands\|brands]], [[data-model#live_sessions\|live_sessions]] | Delete brand + null out references in sessions |

### Brand Storage (`lib/brands/brandStorage.ts`)

| Function | Storage | Purpose |
|----------|---------|---------|
| `uploadBrandLogo` | `brand-logos` bucket | Upload logo PNG to Supabase Storage |
| `getBrandLogoPublicUrl` | `brand-logos` bucket | Get public URL for a stored logo |
| `fetchBrandLogoPngBytes` | `brand-logos` bucket | Download logo bytes for PDF embedding |

### Other Lib Functions

| Function | File | Purpose |
|----------|------|---------|
| `fetchNextUpcomingEventLinks` | `lib/managementApi.ts` | Fetch event links from Anchor API |
| `fetchNextThreeUpcomingEventLinks` | `lib/managementApi.ts` | Fetch next 3 events for PDF QR codes |
| `fetchUpcomingEventDetails` | `lib/managementApi.ts` | Fetch full event detail objects |
| `renderClipboardDocx` | `lib/clipboardDocx.ts` | Render DOCX clipboard/runsheet |
| `generateBingoCards` | `lib/generator.ts` | Generate randomized bingo card grids |

### Auth & Audit

- **Auth:** None. No `getUser()` or auth checks in any repository function or route handler.
- **Audit:** No `logAuditEvent()` calls anywhere in the codebase.
