---
generated: true
last_updated: 2026-05-11T00:00:00Z
source: session-setup
project: music-bingo
---

# Overview

> Auto-generated by session-setup. Manual edits will be overwritten on next refresh.

## Tech Stack

| Layer | Technology | Version |
|-------|-----------|---------|
| Framework | Next.js | 16.1.3 |
| UI | React | 18.3.x |
| Language | TypeScript | 5.5.x |
| Styling | Tailwind CSS | 3.4.x |
| Database | Supabase (PostgreSQL) | Client 2.97.x |
| PDF | pdf-lib | 1.17.x |
| Images | Sharp | 0.33.x |
| QR Codes | qrcode | 1.5.x |
| DOCX | docx | 9.5.x |
| E2E Tests | Playwright | 1.58.x |

## Project Summary

Music Bingo is a multi-device live game host application. A host prepares a bingo session with tracks from Spotify, generates branded PDF/DOCX bingo cards, and runs a real-time game that guests join via a unique session URL. The system syncs game state between host and guests using Supabase Realtime.

**Counts at a glance:**

- **8 pages** (see [[routes#Pages]])
- **19 API route handlers** across 15 route files (see [[routes#API Routes]])
- **2 Supabase tables** + 1 storage bucket (see [[data-model]])
- **0 server actions** -- all mutations go through API route handlers
- **5 external integrations** (see below)

## Key Integrations

| Service | Purpose | Files |
|---------|---------|-------|
| **Spotify Web API** | OAuth, playlist CRUD, track metadata, live playback control | `lib/spotifyWeb.ts`, `lib/spotifyLive.ts`, `app/api/spotify/*` |
| **Supabase** | Session persistence, brand storage, Realtime subscriptions | `lib/supabase.ts`, `lib/live/sessionRepo.ts`, `lib/brands/brandRepo.ts` |
| **Anchor Management API** | Fetch upcoming event details for QR codes in PDFs | `lib/managementApi.ts` |
| **pdf-lib + Sharp** | PDF bingo card generation with embedded images | `lib/pdf.ts`, `app/api/generate/route.ts` |
| **qrcode** | QR code generation for event links in PDFs | `lib/pdf.ts` |
| **docx** | Clipboard/runsheet DOCX export | `lib/clipboardDocx.ts` |

## Auth Model

This project has **no authentication or authorization layer**. There is no middleware, no login, no session-based auth, and no role-based access control. All routes and API endpoints are publicly accessible.

- No `middleware.ts` file exists
- No `getUser()` or `getSupabaseServerClient()` calls in any route or layout
- No `logAuditEvent()` calls anywhere
- Supabase client uses service-role key directly (`lib/supabase.ts`)
- Spotify OAuth tokens are stored in HTTP-only cookies (not user auth -- app-level Spotify access)

This is consistent with the app's use case: a single host operator runs games at a venue, and guests join via public URLs. See [[relationships]] for the full cross-reference map.

## Environment Variables

The project declares **8 environment variables** in `.env.example`. All are server-side only (none use `NEXT_PUBLIC_` prefix):

| Variable | Required | Purpose |
|----------|----------|---------|
| `SPOTIFY_CLIENT_ID` | Yes | Spotify OAuth app ID |
| `SPOTIFY_CLIENT_SECRET` | Yes | Spotify OAuth app secret |
| `SPOTIFY_WEB_REDIRECT_URI` | No | Override Spotify callback URL |
| `SUPABASE_URL` | Yes | Supabase project URL |
| `SUPABASE_SERVICE_ROLE_KEY` | Yes | Supabase service-role key |
| `MANAGEMENT_API_BASE_URL` | No | Anchor Management API base URL |
| `MANAGEMENT_API_TOKEN` | No | Anchor Management API key |
| `MANAGEMENT_PUBLIC_EVENTS_BASE_URL` | No | Public events base URL for QR links |
